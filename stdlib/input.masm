@define INPUT_ADDR = 0x4912

@section text

; read char from keyboard and echo it to screen
.input:
	PUSH16  =INPUT_ADDR
	LDB
	POPR    r0

	PUSHR   r0
	PUSH16  0
	CMP
	JEQ     .input

	; only print non-null characters
	CALL    print
	JMP     .input
