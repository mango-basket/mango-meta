@define INPUT_ADDR = 0x4912

@section text

; read char from keyboard and echo it to screen via INT 0
.input:
	PUSH16  =INPUT_ADDR
	LDB
	POPR    r0

	PUSHR   r0
	PUSH16  0
	CMP
	JEQ     .input

	; r0 has the character, print it via INT 0 (runs in kernel mode)
	INT     0
	JMP     .input
